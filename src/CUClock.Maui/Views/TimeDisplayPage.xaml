<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:CUClock.Shared.ViewModels;assembly=CUClock.Shared"
             x:Class="CUClock.Maui.Views.TimeDisplayPage"
             x:DataType="vm:Announcer">
    <Grid RowDefinitions="*,Auto" RowSpacing="5" Padding="5">
        <Editor x:Name="PhraseBox"
                AutoSize="TextChanges"
                VerticalTextAlignment="Start"
                VerticalOptions="Fill" />
        <FlexLayout JustifyContent="End" Wrap="Wrap"
                    Grid.Row="1" Padding="0,0,0,10">

            <Button Text="Silencio"
                    Command="{Binding Silence}"
                    Margin="3,5">
                <Button.ImageSource>
                    <FontImageSource Glyph="&#xe74f;" />
                </Button.ImageSource>
            </Button>
            <Button Text="Anunciar"
                    Command="{Binding Announce}"
                    Margin="3,5">
                <Button.ImageSource>
                    <FontImageSource Glyph="&#xec92;" />
                </Button.ImageSource>
            </Button>

            <Button Text="{Binding AnteriorText}"
                    Command="{Binding Previous}"
                    Margin="3,5">
                <Button.ImageSource>
                    <FontImageSource Glyph="&#xe892;" />
                </Button.ImageSource>
            </Button>

            <Button Text="Aforismo"
                    Command="{Binding SpeakPhrase}"
                    Margin="3,5">
                <Button.ImageSource>
                    <FontImageSource Glyph="&#xe767;" />
                </Button.ImageSource>
            </Button>

            <Button Text="Repetir"
                    Command="{Binding Repeat}"
                    Margin="3,5">
                <Button.ImageSource>
                    <FontImageSource Glyph="&#xe8ee;" />
                </Button.ImageSource>
            </Button>

            <Button Text="{Binding SiguienteText}"
                    Command="{Binding Next}"
                    Margin="3,5,0,5">
                <Button.ImageSource>
                    <FontImageSource Glyph="&#xe893;" />
                </Button.ImageSource>
            </Button>
        </FlexLayout>
    </Grid>
</ContentPage>